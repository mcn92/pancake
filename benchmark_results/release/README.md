# Release Benchmark Results

This folder is intentionally tracked so final benchmark runs for a release can
be committed and reviewed.

Everything else under `benchmark_results/` remains ignored because it contains
local caches and historical generated output.

Suggested convention:

- write final release artifacts here
- keep file names descriptive
- include the benchmark name, timestamp, and any important parameter variants
